// import shapes
const { Triangle, Square, Circle } = require("./lib/shapes.js")

// testing suite for shapes
describe('Shapes', () => {
    // checks triangle
    describe('triangle', () => {
        it('should render an svg triangle', () => {
            logo = new Square(response.textInput, response.textColorInput, response.shapeColorInput);
        });
    });
  
    // checks square
    describe('square', () => {
        it('should render an svg square', () => {
        
        });
    })

    // checks circle
    describe('circle', () => {
        it('should render an svg circle', () => {
    
        });
    })
});